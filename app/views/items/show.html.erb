<h1 style="font-weight: bold" class="text-center">
  <%= @item.title %>
</h1>

<p class="text-center"><%= @item.description %></p>

<p style="font-size: 18px" class="text-center">
  <%= link_to 'Editar', edit_collection_album_item_path(@collection, @album, @item),
  class: 'btn btn-warning' %>

  <%= link_to 'Deletar', collection_album_item_path(@collection, @album, @item), method: :delete,
  data: { confirm: 'Você tem certeza que deseja Deletar?' }, class: 'btn btn-danger' %>

  <%= link_to 'Voltar', collection_album_items_path(@collection, @album),
  class: 'btn btn-primary' %>
</p>
